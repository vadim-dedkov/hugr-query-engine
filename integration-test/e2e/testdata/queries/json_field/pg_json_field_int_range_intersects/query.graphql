{
  pg_store {
    json_field_demo(
      filter: { data: { field: { path: "span.i4", intRange: { intersects: "[15,25)" } } } }
      order_by: [{ field: "id", direction: ASC }]
    ) {
      id
    }
  }
}
